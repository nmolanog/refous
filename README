This project is to solve problems present in math subjects in Refous school.
To install:

make sure library devtools is instaled. if not just type

install.packages("devtools")

To install the package type:

install_github("nmolanog/refous") 
